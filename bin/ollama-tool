#!/bin/bash

# Wrapper script for GitHub Dotfiles Ollama
# Assumes github-dotfiles-ollama is in the same directory as this script's parent

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" &> /dev/null && pwd)"
OLLAMA_DIR="$SCRIPT_DIR/../ollama"

"$OLLAMA_DIR/run.sh" "$@"
