#!/bin/bash

# Wrapper script for Ollama tool
# Assumes ollama-tool is in the same directory as this script's parent

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" &> /dev/null && pwd)"
OLLAMA_DIR="$SCRIPT_DIR/../ollama-tool"

"$OLLAMA_DIR/run.sh" "$@"